/target
**/*.rs.bk
*.pdb
.DS_Store
# local DAG stores / scratch
*.memdag/
/data

# TLA+ TLC local artifacts (jar fetched per-run; traces are transient)
specs/tla2tools.jar
specs/*_TTrace_*
specs/states/

# OSS history-scrub replacements (contains real infra values — never commit)
oss-scrub-replacements.txt
*.scrub-replacements.txt

# mutation-testing artifacts (regeneratable; never tracked/published)
mutants.out/
mutants.out.old/

# ─────────────────────────────────────────────────────────────────────
# Open-source hygiene — secret material & internal records (added 2026-09)
# History was scrubbed of these; this block prevents re-introduction.
# ─────────────────────────────────────────────────────────────────────

# Secret material — never in a public repo
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.p8
*.der
id_rsa
id_ed25519
*_private*.pem
*private_key*
*jwks*.json
**/secrets.json
**/secrets.yaml
**/secrets.yml
**/secrets.toml
mnemonic*
*seed-phrase*
*seed_phrase*

# Internal process / institutional-knowledge records — not for public repos
handoffs/
**/*HANDOFF*
**/*_HANDOFF*
**/*INCIDENT*
**/*EXEC_STATUS*
**/*COUNSEL*
**/*DEPLOY_LOG*
