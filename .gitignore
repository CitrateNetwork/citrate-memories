/target
**/*.rs.bk
*.pdb
.DS_Store
# local DAG stores / scratch
*.memdag/
/data

# TLA+ TLC local artifacts (jar fetched per-run; traces are transient)
specs/tla2tools.jar
specs/*_TTrace_*
specs/states/

# OSS history-scrub replacements (contains real infra values — never commit)
oss-scrub-replacements.txt
*.scrub-replacements.txt
